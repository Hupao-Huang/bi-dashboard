# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.gstack/

# server binaries
server/*.exe
server/*.exe~
server/*.exe.bak-*
server/cmd/**/*.exe
server/*.log
server/*.log.old
server/archive/
server/logs/
server/manual-sync-*.log

# 一次性探针/调试工具
server/cmd/probe-trade-fields/
server/trade-fields-dump.json

# git filter-repo 备份
.git.bak-*

# 前端 npx serve 日志
frontend-serve.log

# server config (contains passwords)
server/config.json

# RPA uploads / pending imports (中间文件，源数据在 Z:\信息部\RPA_集团数据看板\)
server/uploads/

# npx serve log
serve.log

# backup scripts (hardcoded DB password)
server/backup-mysql.bat
server/backup-mysql.ps1
server/test_backup.bat
server/test_backup.*
server/check-trade-gaps.sh

# go cache
server/.gocache/

# lock files
server/*.lock

# temp files
*.tmp
Thumbs.db

# logs outside server
frontend.log
*.err.log
.codex-*.log
sync-trades-*.log
/bi-server.stderr.log
/bi-server.stdout.log

# codex / agents
.agents/
.codex-*
skills-lock.json

# claude
.claude/

# audit 临时证据 (截图/body 快照/.bak 备份, 不进 git)
/audit-*/

# root 构建日志 (server/*.log 只盖 server/, 这里盖 repo root)
/*.log

# .err dump (区别于已盖的 *.err.log)
*.err
