-- v1.10 公告
UPDATE notices SET is_pinned=0 WHERE is_pinned=1;

INSERT INTO notices (title, content, is_pinned, created_by, created_at) VALUES (
'v1.10 反馈管理 视觉对齐 + 体验升级',
'本次完成内容:

【顶部新增 4 个总览卡片】
进反馈管理一眼看到 待处理 / 处理中 / 已解决 / 已关闭 4 个核心数。
"待处理" 大于 0 时数字自动加粗变橙, 不会再漏掉新反馈。

【反馈列表 视觉对齐】
- 每行加左色条: 待处理=橙 / 处理中=蓝 / 已解决=绿 / 已关闭=灰
- 选中行背景 = 主状态色 5% 透明度 (和用户管理/角色管理同一套设计语言)
- 状态标签从 antd 默认颜色改成 浅底+边框+主色字 的克制风格
- 标题列改成 "标题+提交人姓名" 上下两行紧凑排版

【顶部筛选区精简】
- 删掉单独的筛选 Card (之前一行只放 1 个下拉 1 个按钮, 浪费)
- 搜索框 + 状态筛选 合并到反馈列表 标题栏右侧
- 删掉冗余的 "刷新" 按钮 (筛选会自动重拉)
- 加搜索框: 按 标题/内容/提交人姓名/账号 实时过滤

【反馈详情 弹窗重做】
- 顶部新增 "反馈档案卡": 白底+左色条+标题+状态+提交人+时间+页面链接
- "提交页面" 改成可直接点击跳转的链接 (LinkOutlined 图标), 管理员
  看到反馈不用再复制 URL 手动开页面
- 截图缩略图改成更紧凑的 88x88 排列, 支持点击大图预览
- 回复输入框 placeholder 加 "(提交人后续将能看到)" 提示

【真 bug 修复】
- 之前 "保存" 按钮校验逻辑形同虚设 (打开详情时已自动填了 reply,
  导致 if(!replyText) 永远为假, 任何点击都会写库)。本次改成
  "对比 原值 vs 新值, 真有改动才提交"。
- 之前改状态会顺带把 reply 字段重新覆盖一遍现有内容, 现在只在
  reply 真改了 才传 reply 字段。
- 列表附件原来用 Badge 数字角标, 看不出是图片。现在 inline 显示
  前 2 张缩略图, 多余 N 张用 "+N" 提示, 直接点击大图预览。
- 提交时间加排序按钮, 默认按时间倒序。

【1366 笔记本响应式】
列表在窄屏自动隐藏 提交页面/附件/提交时间 列, 保住 标题/状态/内容
主信息。≥1600 大屏继续显示全部列。

不影响数据口径与定时任务运行',
1, 'system', NOW());

SELECT id, title, is_pinned, DATE_FORMAT(created_at, '%Y-%m-%d %H:%i') AS '时间'
FROM notices WHERE is_pinned=1;
